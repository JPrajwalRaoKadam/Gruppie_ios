import UIKit

class AddManagementMoreCell: UITableViewCell {

    // Example outlet for a label
    
    override func awakeFromNib() {
        super.awakeFromNib()
        // Initialization code (e.g., setting up default states)
    }

    override func setSelected(_ selected: Bool, animated: Bool) {
        super.setSelected(selected, animated: animated)

        // Configure the view for the selected state
    }

    // You can add custom functions to configure the cell if needed
  
}
 
